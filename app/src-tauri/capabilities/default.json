{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: allows running the uci-engine sidecar (chess-tauri-zero's PyTorch AlphaZero engine over UCI) and nothing else.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/uci-engine",
          "sidecar": true,
          "args": []
        }
      ]
    }
  ]
}
